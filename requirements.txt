# =============================================================================
# Zentro-ZIMS — Python Dependencies
# =============================================================================
# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==23.0.0          # Production WSGI/ASGI server

# Database
sqlalchemy==2.0.35
alembic==1.14.0
psycopg2-binary==2.9.9    # PostgreSQL adapter

# Pydantic + settings
pydantic==2.9.2
pydantic-settings==2.5.2

# Auth / security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12  # Form / file uploads

# Environment
python-dotenv==1.0.1

# Scheduling (Phase 1-2)
apscheduler==3.10.4

# AI (Phase 3+)
openai==1.51.0

# WhatsApp / SMS
twilio==9.3.2

# Email
sendgrid==6.11.0
email-validator==2.2.0

# HTTP client
httpx==0.27.2

# Payments (Phase 3+)
stripe==11.1.0

# Excel import / export
openpyxl==3.1.5
pandas==2.2.3

# Cache / task broker (Phase 1 foundation, Phase 3 Celery)
redis==5.0.8

celery==5.4.0             # Background task queue (Phase 3)

# Monitoring / observability (Phase 4)
# prometheus-client==0.20.0
# opentelemetry-api==1.25.0

# Testing (Phase 1+)
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2             # Already listed; used by TestClient
factory-boy==3.3.1

# Development
rich==13.8.1              # Pretty logs / CLI output
