import type { ReactNode } from "react";

export const metadata = { title: "Welcome · Zentro" };

export default function OnboardingLayout({ children }: { children: ReactNode }) {
  return children;
}
